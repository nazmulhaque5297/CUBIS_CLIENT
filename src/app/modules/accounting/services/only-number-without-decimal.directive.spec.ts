import { OnlyNumberWithoutDecimalDirective } from './only-number-without-decimal.directive';

describe('OnlyNumberWithoutDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberWithoutDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
